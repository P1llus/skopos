name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  ci:
    name: ci
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true
      - name: make schema-doc-check
        run: |
          make schema-doc-check
      - name: make json-schema-check
        run: |
          make json-schema-check
      - name: Install golangci-lint
        run: |
          curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/HEAD/install.sh \
            | sh -s -- -b "$(go env GOPATH)/bin" v2.12.2
      - name: make ci
        run: make ci



  # release-snapshot disabled until first release; re-enable when going public.
  # release-snapshot:
  #   name: release-snapshot
  #   runs-on: ubuntu-latest
  #   timeout-minutes: 15
  #   steps:
  #     - uses: actions/checkout@v6
  #       with:
  #         fetch-depth: 0
  #     - uses: actions/setup-go@v6
  #       with:
  #         go-version-file: go.mod
  #         cache: true
  #     - name: goreleaser check
  #       uses: goreleaser/goreleaser-action@v6
  #       with:
  #         version: "~> v2"
  #         args: check
  #     - name: goreleaser release --snapshot
  #       uses: goreleaser/goreleaser-action@v6
  #       with:
  #         version: "~> v2"
  #         args: release --snapshot --clean --skip=publish
