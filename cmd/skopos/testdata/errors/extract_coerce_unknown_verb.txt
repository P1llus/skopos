# errors/extract_coerce_unknown_verb — invalid spec; validate exits 1 with diagnostic
# matching the substring tested by TestValidateErrors in
# schema/fixtures_test.go.

! exec skopos validate -i spec.yml
! stderr .
stdout 'unknown coerce verb'

-- spec.yml --
ir_version: "1"
auth:
  none: {}
requests:
  - method: GET
    path: /api/v1/events
    extract:
      - name: tag
        from: response.body.foo
        coerce: not_a_verb
response:
  decode: json
  events_at: response.body.events
pagination:
  none: {}
progress:
  stateless: {}
