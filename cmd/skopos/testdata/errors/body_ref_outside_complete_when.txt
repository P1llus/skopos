# errors/body_ref_outside_complete_when — invalid spec; validate exits 1 with diagnostic
# matching the substring tested by TestValidateErrors in
# schema/fixtures_test.go.

! exec skopos validate -i spec.yml
! stderr .
stdout 'body namespace was removed'

-- spec.yml --
ir_version: "1"
auth:
  none: {}
requests:
  - method: GET
    path: /api/v1/events
    query:
      since: {ref: body.timestamp}
response:
  decode: json
  events_at: response.body.events
pagination:
  none: {}
progress:
  stateless: {}
