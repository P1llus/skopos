# errors/from_progress_strategy_mismatch_latest_on_window — invalid spec; validate exits 1 with diagnostic
# matching the substring tested by TestValidateErrors in
# schema/fixtures_test.go.

! exec skopos validate -i spec.yml
! stderr .
stdout 'does not match the active progress strategy'

-- spec.yml --
ir_version: "1"
auth:
  none: {}
requests:
  - method: POST
    path: /api/v1/search
    body:
      json:
        since: {from_progress: latest_timestamp}
response:
  decode: json
  events_at: response.body.events
pagination:
  none: {}
progress:
  time_window:
    initial_offset: "24h"
