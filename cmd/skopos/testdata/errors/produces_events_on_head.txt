# errors/produces_events_on_head — invalid spec; validate exits 1 with diagnostic
# matching the substring tested by TestValidateErrors in
# schema/fixtures_test.go.

! exec skopos validate -i spec.yml
! stderr .
stdout 'HEAD has no response body'

-- spec.yml --
ir_version: "1"
auth:
  none: {}
requests:
  - method: HEAD
    path: /api/v1/events
    produces_events: true
response:
  decode: json
  events_at: response.body.events
pagination:
  none: {}
progress:
  stateless: {}
