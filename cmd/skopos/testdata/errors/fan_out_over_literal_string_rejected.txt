# errors/fan_out_over_literal_string_rejected — invalid spec; validate exits 1 with diagnostic
# matching the substring tested by TestValidateErrors in
# schema/fixtures_test.go.

! exec skopos validate -i spec.yml
! stderr .
stdout 'fan_out\.over must resolve to a list'

-- spec.yml --
ir_version: "1"
auth:
  none: {}
requests:
  - id: list
    method: GET
    path: /api/v1/things
  - method: GET
    path: /api/v1/details
    fan_out:
      over: items
      as: it
response:
  decode: json
  events_at: events
pagination:
  none: {}
progress:
  stateless: {}
