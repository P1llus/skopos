# errors/steps_ref_missing_body_segment — invalid spec; validate exits 1 with diagnostic
# matching the substring tested by TestValidateErrors in
# schema/fixtures_test.go.

! exec skopos validate -i spec.yml
! stderr .
stdout 'steps ref must include the body segment'

-- spec.yml --
ir_version: "1"
auth:
  none: {}
requests:
  - id: login
    method: POST
    path: /api/v1/login
  - method: GET
    path: /api/v1/events
    query:
      token: {ref: steps.login}
response:
  decode: json
  events_at: events
pagination:
  none: {}
progress:
  stateless: {}
