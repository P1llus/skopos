# errors/state_values_on_non_enum — invalid spec; validate exits 1 with diagnostic
# matching the substring tested by TestValidateErrors in
# schema/fixtures_test.go.

! exec skopos validate -i spec.yml
! stderr .
stdout 'values is only valid when type: enum'

-- spec.yml --
ir_version: "1"
state:
  fields:
    mode:
      type: string
      values: [a, b]
auth:
  none: {}
requests:
  - method: GET
    path: /api/v1/events
response:
  decode: json
  events_at: events
pagination:
  none: {}
progress:
  stateless: {}
