# errors/defaults_unknown_state_ref — invalid spec; validate exits 1 with diagnostic
# matching the substring tested by TestValidateErrors in
# schema/fixtures_test.go.

! exec skopos validate -i spec.yml
! stderr .
stdout 'not declared in state\.fields'

-- spec.yml --
ir_version: "1"
defaults:
  base_url: {ref: state.missing}
auth:
  none: {}
requests:
  - method: GET
    path: /api/v1/events
response:
  decode: json
  events_at: response.body.events
pagination:
  none: {}
progress:
  stateless: {}
