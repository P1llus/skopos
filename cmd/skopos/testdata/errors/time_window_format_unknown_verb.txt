# errors/time_window_format_unknown_verb — invalid spec; validate exits 1 with diagnostic
# matching the substring tested by TestValidateErrors in
# schema/fixtures_test.go.

! exec skopos validate -i spec.yml
! stderr .
stdout 'unknown format verb'

-- spec.yml --
ir_version: "1"
auth:
  none: {}
requests:
  - method: POST
    path: /api/v1/search
    body:
      json:
        since: {from_progress: window_start}
response:
  decode: json
  events_at: response.body.events
pagination:
  none: {}
progress:
  time_window:
    initial_offset: "24h"
    format: rcf3339
