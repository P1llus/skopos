# yaml-language-server: $schema=https://raw.githubusercontent.com/p1llus/skopos/main/docs/schema/v1/skopos.schema.json
# fanout — per-item fan-out over a list.
#
# Demonstrates: requests[].fan_out with merge: flatten. The list step
# returns incidents; the detail step runs once per incident with the
# author-chosen `incident` binding (`{ref: incident.id}` resolves to the
# current item). Per-item responses are JSON lists; merge: flatten
# concatenates them so the producer iteration emits one event per
# detail-row across every incident.
#
# Mapping to the runner:
#   - {ref: incident.id} resolves against the per-item binding while the
#     fan_out step iterates. The binding name comes from fan_out.as, so
#     authors pick what reads well (incident, blob, asset, …).
#   - The fan-out step is the producer: it sets events_at to the empty
#     path, so the merged body root is the events stream.
#   - fan_out and requests[].cache are mutually exclusive (the validator
#     enforces this); per-item errors run through the same on_status /
#     error.mode dispatcher as a single-request step.

ir_version: "1"

state:
  url:
    type: url
    default: "http://localhost:9999"
  api_key:
    type: secret
    default: "test-bearer-token-12345"
  last_timestamp:
    type: timestamp
    default: {subtract: [{now: true}, "720h"]}

auth:
  bearer:
    token: {ref: state.api_key}

requests:
  - id: list
    method: GET
    url: "${state.url}/fanout/incidents"

  - id: detail
    method: GET
    url: "${state.url}/fanout/incidents/${incident.id}/events"
    fan_out:
      over:  {ref: steps.list.body.incidents}
      as:    incident
      merge: flatten
    # merge: flatten produces a JSON list as the producer body; events_at
    # is the empty path (body root) so each list element is one event.
    events_at: ""

pagination:
  none: {}

progress:
  - to: state.last_timestamp
    from: {max: [{ref: state.last_timestamp}, {max: {ref: events.*.timestamp}}]}

error:
  mode: standard
