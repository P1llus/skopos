{
  "$comment": "Code generated by tools/gen-json-schema. DO NOT EDIT.",
  "$id": "https://raw.githubusercontent.com/p1llus/skopos/main/docs/schema/v1/skopos.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "APIKeyAuth": {
      "additionalProperties": false,
      "description": "APIKeyAuth sends the key in a named header (or query parameter when InQuery is true).",
      "properties": {
        "header": {
          "description": "Header is the wire name of the header (or query parameter when InQuery is true) that carries the credential.",
          "type": "string"
        },
        "in_query": {
          "description": "InQuery, when true, sends the credential as a URL query parameter instead of an HTTP header.",
          "type": "boolean"
        },
        "value": {
          "$ref": "#/definitions/Value",
          "description": "Value resolves to the credential at request time."
        }
      },
      "required": [
        "header",
        "value"
      ],
      "type": "object"
    },
    "AdvanceWrite": {
      "additionalProperties": false,
      "description": "AdvanceWrite is one entry in CustomPagination.Advance — a {to, from, regex?, coerce?} write evaluated and applied per-page.",
      "properties": {
        "coerce": {
          "description": "Coerce, when set, applies a format verb to the resolved value before writing.",
          "type": "string"
        },
        "from": {
          "$ref": "#/definitions/Value",
          "description": "From is the Value to resolve and write."
        },
        "regex": {
          "description": "Regex is the optional regular expression applied to the resolved string before writing.",
          "type": "string"
        },
        "to": {
          "$ref": "#/definitions/Path",
          "description": "To is the state.\u003cname\u003e destination (per-drain)."
        }
      },
      "required": [
        "from",
        "to"
      ],
      "type": "object"
    },
    "Auth": {
      "additionalProperties": false,
      "description": "Auth is the discriminated-union auth block. Exactly one variant key must be present.",
      "oneOf": [
        {
          "required": [
            "api_key"
          ]
        },
        {
          "required": [
            "basic"
          ]
        },
        {
          "required": [
            "bearer"
          ]
        },
        {
          "required": [
            "custom"
          ]
        },
        {
          "required": [
            "multi_mode"
          ]
        },
        {
          "required": [
            "none"
          ]
        },
        {
          "required": [
            "oauth2"
          ]
        },
        {
          "required": [
            "sigv4"
          ]
        }
      ],
      "properties": {
        "api_key": {
          "$ref": "#/definitions/APIKeyAuth",
          "description": "APIKey selects the named-header (or named-query) API-key variant."
        },
        "basic": {
          "$ref": "#/definitions/BasicAuth",
          "description": "Basic selects the HTTP basic-auth variant."
        },
        "bearer": {
          "$ref": "#/definitions/BearerAuth",
          "description": "Bearer selects the bearer-token variant."
        },
        "custom": {
          "$ref": "#/definitions/CustomAuth",
          "description": "Custom selects the single-custom-header variant."
        },
        "multi_mode": {
          "$ref": "#/definitions/MultiModeAuth",
          "description": "MultiMode dispatches between auth strategies at runtime via predicates."
        },
        "none": {
          "description": "None selects the no-auth variant.",
          "type": "object"
        },
        "oauth2": {
          "$ref": "#/definitions/OAuth2Auth",
          "description": "OAuth2 selects the OAuth2 variant (client_credentials / password_grant)."
        },
        "sigv4": {
          "$ref": "#/definitions/SigV4Auth",
          "description": "SigV4 selects the AWS Signature Version 4 request-signing variant."
        }
      },
      "type": "object"
    },
    "AuthBranch": {
      "additionalProperties": false,
      "description": "AuthBranch is one arm of a multi_mode auth dispatch.",
      "properties": {
        "auth": {
          "$ref": "#/definitions/Auth",
          "description": "Auth is the variant applied while the predicate is true."
        },
        "when": {
          "$ref": "#/definitions/Predicate",
          "description": "When is the predicate that selects this branch."
        }
      },
      "required": [
        "auth",
        "when"
      ],
      "type": "object"
    },
    "BasicAuth": {
      "additionalProperties": false,
      "description": "BasicAuth sends \"Authorization: Basic \u003cbase64(username:password)\u003e\".",
      "properties": {
        "password": {
          "$ref": "#/definitions/Value",
          "description": "Password is the basic-auth password."
        },
        "username": {
          "$ref": "#/definitions/Value",
          "description": "Username is the basic-auth user name."
        }
      },
      "required": [
        "password",
        "username"
      ],
      "type": "object"
    },
    "BearerAuth": {
      "additionalProperties": false,
      "description": "BearerAuth sends \"Authorization: Bearer \u003ctoken\u003e\" on every request.",
      "properties": {
        "token": {
          "$ref": "#/definitions/Value",
          "description": "Token resolves to the bearer token at request time."
        }
      },
      "required": [
        "token"
      ],
      "type": "object"
    },
    "Body": {
      "additionalProperties": false,
      "description": "Body is the discriminated-union request body. Exactly one variant key must be present.",
      "oneOf": [
        {
          "required": [
            "form"
          ]
        },
        {
          "required": [
            "json"
          ]
        },
        {
          "required": [
            "raw"
          ]
        }
      ],
      "properties": {
        "form": {
          "additionalProperties": {
            "$ref": "#/definitions/Value"
          },
          "description": "Form selects the application/x-www-form-urlencoded variant.",
          "type": "object"
        },
        "json": {
          "additionalProperties": {
            "$ref": "#/definitions/Value"
          },
          "description": "JSON selects the application/json variant.",
          "type": "object"
        },
        "raw": {
          "$ref": "#/definitions/Value",
          "description": "Raw selects the literal-body variant."
        }
      },
      "type": "object"
    },
    "Cache": {
      "additionalProperties": false,
      "description": "Cache is the unified cache block shared by OAuth2 grants (auth.oauth2.\u003cgrant\u003e.cache) and step-level caches (requests[].cache). The captured value lives in cache.\u003cname\u003e; the slot is process memory only and is never persisted.",
      "properties": {
        "buffer": {
          "description": "Buffer is a Go-style duration. Re-fetch when the remaining lifetime falls below this.",
          "type": "string"
        },
        "expires_at": {
          "$ref": "#/definitions/Value",
          "description": "ExpiresAt is the Value resolving to a time.Time. Accepts a {ref: ..., default: ...} fallback for APIs that return no explicit expiry."
        },
        "to": {
          "$ref": "#/definitions/Path",
          "description": "To is the cache.\u003cname\u003e destination slot. Reads use {ref: cache.\u003cname\u003e}."
        }
      },
      "required": [
        "buffer",
        "expires_at",
        "to"
      ],
      "type": "object"
    },
    "ClientCredentialsGrant": {
      "additionalProperties": false,
      "description": "ClientCredentialsGrant is the OAuth2 client_credentials flow.",
      "properties": {
        "audience": {
          "description": "Audience is the optional RFC 8693 audience claim sent in the token request.",
          "type": "string"
        },
        "cache": {
          "$ref": "#/definitions/Cache",
          "description": "Cache, when set, writes the captured access token into a cache.\u003cname\u003e slot."
        },
        "client_id": {
          "$ref": "#/definitions/Value",
          "description": "ClientID identifies the client to the authorization server."
        },
        "client_secret": {
          "$ref": "#/definitions/Value",
          "description": "ClientSecret authenticates the client to the authorization server."
        },
        "scopes": {
          "description": "Scopes is the optional space-separated OAuth2 scope list (RFC 6749 §3.3).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "token_url": {
          "$ref": "#/definitions/Value",
          "description": "TokenURL is the OAuth2 token endpoint."
        }
      },
      "required": [
        "client_id",
        "client_secret",
        "token_url"
      ],
      "type": "object"
    },
    "CounterPagination": {
      "additionalProperties": false,
      "description": "CounterPagination advances via a client-incremented counter.",
      "properties": {
        "start": {
          "$ref": "#/definitions/Value",
          "description": "Start is the optional starting Value. Defaults to 1 (page number); use 0 for offset-style pagination."
        },
        "step": {
          "$ref": "#/definitions/Value",
          "description": "Step is the optional increment per accepted page. Defaults to 1. For offset-style pagination, set to {ref: state.page_size}."
        },
        "terminate_when": {
          "$ref": "#/definitions/Predicate",
          "description": "TerminateWhen overrides the default termination predicate (short-page detection: events.count \u003c step)."
        },
        "to": {
          "$ref": "#/definitions/Path",
          "description": "To is the state.\u003cname\u003e destination (per-drain, typed int)."
        }
      },
      "required": [
        "to"
      ],
      "type": "object"
    },
    "CursorTokenPagination": {
      "additionalProperties": false,
      "description": "CursorTokenPagination advances via a next-cursor token returned by the server.",
      "properties": {
        "from": {
          "$ref": "#/definitions/Path",
          "description": "From is the Path to the next-cursor field, rooted at response.body.\u003cpath\u003e, response.header.\u003cname\u003e, or steps.\u003cid\u003e.body.\u003cpath\u003e."
        },
        "terminate_when": {
          "$ref": "#/definitions/Predicate",
          "description": "TerminateWhen overrides the default termination predicate ({not: {present: \u003cfrom\u003e}})."
        },
        "to": {
          "$ref": "#/definitions/Path",
          "description": "To is the state.\u003cname\u003e destination (per-drain)."
        }
      },
      "required": [
        "from",
        "to"
      ],
      "type": "object"
    },
    "CustomAuth": {
      "additionalProperties": false,
      "description": "CustomAuth sends a single custom header with an arbitrary value.",
      "properties": {
        "header": {
          "description": "Header is the wire name of the credential-bearing header.",
          "type": "string"
        },
        "value": {
          "$ref": "#/definitions/Value",
          "description": "Value resolves to the credential at request time."
        }
      },
      "required": [
        "header",
        "value"
      ],
      "type": "object"
    },
    "CustomPagination": {
      "additionalProperties": false,
      "description": "CustomPagination is the author-controlled primitive form for APIs that don't fit the named variants.",
      "properties": {
        "advance": {
          "description": "Advance is the ordered list of writes that run when TerminateWhen evaluates false.",
          "items": {
            "$ref": "#/definitions/AdvanceWrite"
          },
          "type": "array"
        },
        "terminate_when": {
          "$ref": "#/definitions/Predicate",
          "description": "TerminateWhen is required for custom pagination — it states the termination condition explicitly."
        }
      },
      "required": [
        "advance",
        "terminate_when"
      ],
      "type": "object"
    },
    "Diagnostic": {
      "additionalProperties": false,
      "description": "Diagnostic is a single structural-validation finding.",
      "properties": {
        "column": {
          "description": "Column is the source-column counterpart to Line, with the same \"set by callers, not Validate\" contract.",
          "type": "integer"
        },
        "hint": {
          "description": "Hint is an optional follow-up sentence pointing at the fix.",
          "type": "string"
        },
        "line": {
          "description": "Line is an optional source-location annotation. Validate never sets it; callers that wrap Validate around raw YAML/JSON bytes (the CLI, the HTTP API) may set it from YAML parser error messages for richer output.",
          "type": "integer"
        },
        "message": {
          "description": "Message is the human-readable error or warning text.",
          "type": "string"
        },
        "path": {
          "description": "Path is a dotted IR path with array indices, e.g. \"requests[0].extract[1].from\". Empty for document-wide findings.",
          "type": "string"
        },
        "severity": {
          "description": "Severity is \"error\" or \"warning\".",
          "type": "string"
        }
      },
      "required": [
        "message",
        "path",
        "severity"
      ],
      "type": "object"
    },
    "ErrorBlock": {
      "additionalProperties": false,
      "description": "ErrorBlock configures how non-success HTTP responses (and network / decode failures) are surfaced.",
      "properties": {
        "include_body": {
          "description": "IncludeBody, when true, includes the response body in the surfaced error message. Off by default.",
          "type": "boolean"
        },
        "mode": {
          "description": "Mode is the document-level dispatcher: \"standard\" (default), \"warn\", or \"fail\".",
          "enum": [
            "standard",
            "warn",
            "fail"
          ],
          "type": "string"
        }
      },
      "required": [
        "mode"
      ],
      "type": "object"
    },
    "ExtractVar": {
      "additionalProperties": false,
      "description": "ExtractVar names a value to pull from a response into the state or extract namespace.",
      "properties": {
        "coerce": {
          "description": "Coerce, when set, applies a format verb to the extracted value before storing it.",
          "type": "string"
        },
        "from": {
          "$ref": "#/definitions/Path",
          "description": "From is the namespace-rooted Path locating the value to capture. One of response.body.\u003cpath\u003e, response.header.\u003cname\u003e, steps.\u003cid\u003e.body.\u003cpath\u003e, steps.\u003cid\u003e.header.\u003cname\u003e."
        },
        "regex": {
          "description": "Regex is an optional regular expression applied to the resolved string before writing.",
          "type": "string"
        },
        "to": {
          "$ref": "#/definitions/Path",
          "description": "To is the destination slot: either state.\u003cname\u003e (persistent — must be declared under state:) or extract.\u003cname\u003e (per-iteration, no declaration needed). The namespace prefix decides persistence."
        }
      },
      "required": [
        "from",
        "to"
      ],
      "type": "object"
    },
    "FanOut": {
      "additionalProperties": false,
      "description": "FanOut lifts a step into a per-item iteration over a list.",
      "properties": {
        "as": {
          "description": "As is the author-chosen per-item binding name; refs inside the step use {ref: \u003cas\u003e.\u003cpath\u003e}.",
          "type": "string"
        },
        "merge": {
          "description": "Merge controls how the per-iteration outputs combine: \"flatten\" (default) or \"wrap\".",
          "enum": [
            "flatten",
            "wrap"
          ],
          "type": "string"
        },
        "over": {
          "$ref": "#/definitions/Value",
          "description": "Over resolves to the list to iterate; must be list-shaped at evaluation time."
        }
      },
      "required": [
        "as",
        "over"
      ],
      "type": "object"
    },
    "FieldDecl": {
      "additionalProperties": false,
      "description": "FieldDecl is the declaration for a single state field. A field's lifetime (operator-config / per-drain scratch / persistent) is inferred from its write sites; it is NOT carried on the declaration.",
      "properties": {
        "default": {
          "$ref": "#/definitions/Value",
          "description": "Default is the Value used when no operator input is supplied. Any Value form, not just a literal — composing {now: true}, {subtract: [...]}, {ref: ...}, etc. is permitted."
        },
        "format": {
          "description": "Format is the wire-format hint for Type == \"timestamp\" or Type == \"duration\". One of the closed-set verbs (rfc3339, rfc3339nano, unix_seconds, unix_millis) or a Go layout string. Ignored on other types.",
          "type": "string"
        },
        "type": {
          "description": "Type is the field's declared shape. One of: string | int | bool | secret | duration | timestamp | url | enum.",
          "enum": [
            "string",
            "int",
            "bool",
            "secret",
            "duration",
            "timestamp",
            "url",
            "enum"
          ],
          "type": "string"
        },
        "values": {
          "description": "Values is the allowed enumeration. Only valid when Type == \"enum\".",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "type"
      ],
      "type": "object"
    },
    "MultiModeAuth": {
      "additionalProperties": false,
      "description": "MultiModeAuth dispatches between auth strategies at runtime based on a Predicate over any namespace.",
      "properties": {
        "branches": {
          "description": "Branches is the ordered list of (when, auth) arms. The first arm whose predicate is true wins.",
          "items": {
            "$ref": "#/definitions/AuthBranch"
          },
          "type": "array"
        },
        "default": {
          "$ref": "#/definitions/Auth",
          "description": "Default is the bare Auth value applied when no branch matches."
        }
      },
      "required": [
        "branches",
        "default"
      ],
      "type": "object"
    },
    "NextURLPagination": {
      "additionalProperties": false,
      "description": "NextURLPagination advances via a fully-formed next-page URL.",
      "properties": {
        "capture": {
          "description": "Capture is the optional capture-group index for Regex (1-based).",
          "type": "integer"
        },
        "from": {
          "$ref": "#/definitions/Path",
          "description": "From is the Path to the next-URL field, rooted at response.body.\u003cpath\u003e, response.header.\u003cname\u003e, or steps.\u003cid\u003e.body.\u003cpath\u003e."
        },
        "regex": {
          "description": "Regex is the optional regular expression applied to the resolved string before writing — used for parsing `Link: \u003curl\u003e; rel=\"next\"` and similar.",
          "type": "string"
        },
        "terminate_when": {
          "$ref": "#/definitions/Predicate",
          "description": "TerminateWhen overrides the default termination predicate ({not: {present: \u003cfrom\u003e}})."
        },
        "to": {
          "$ref": "#/definitions/Path",
          "description": "To is the state.\u003cname\u003e destination (per-drain, typed url)."
        }
      },
      "required": [
        "from",
        "to"
      ],
      "type": "object"
    },
    "OAuth2Auth": {
      "additionalProperties": false,
      "description": "OAuth2Auth selects the OAuth2 grant type. Exactly one grant key must be present.",
      "oneOf": [
        {
          "required": [
            "client_credentials"
          ]
        },
        {
          "required": [
            "password_grant"
          ]
        }
      ],
      "properties": {
        "client_credentials": {
          "$ref": "#/definitions/ClientCredentialsGrant",
          "description": "ClientCredentials selects the OAuth2 client_credentials flow."
        },
        "password_grant": {
          "$ref": "#/definitions/PasswordGrant",
          "description": "PasswordGrant selects the OAuth2 password grant (RFC 6749 §4.3)."
        }
      },
      "type": "object"
    },
    "Pagination": {
      "additionalProperties": false,
      "description": "Pagination is the discriminated-union pagination block. Exactly one variant key must be present.",
      "oneOf": [
        {
          "required": [
            "counter"
          ]
        },
        {
          "required": [
            "cursor_token"
          ]
        },
        {
          "required": [
            "custom"
          ]
        },
        {
          "required": [
            "next_url"
          ]
        },
        {
          "required": [
            "none"
          ]
        }
      ],
      "properties": {
        "counter": {
          "$ref": "#/definitions/CounterPagination",
          "description": "Counter advances via a client-incremented counter (page number or offset)."
        },
        "cursor_token": {
          "$ref": "#/definitions/CursorTokenPagination",
          "description": "CursorToken advances via a next-cursor token returned in the body or a header."
        },
        "custom": {
          "$ref": "#/definitions/CustomPagination",
          "description": "Custom exposes the primitive form: an author-supplied list of advance writes plus an author-supplied terminate_when predicate."
        },
        "next_url": {
          "$ref": "#/definitions/NextURLPagination",
          "description": "NextURL advances via a fully-formed next-page URL returned in the body or a header (e.g. Link: \u003curl\u003e; rel=\"next\")."
        },
        "none": {
          "description": "None disables pagination — exactly one page is fetched per drain.",
          "type": "object"
        }
      },
      "type": "object"
    },
    "PasswordGrant": {
      "additionalProperties": false,
      "description": "PasswordGrant is the OAuth2 password grant (RFC 6749 §4.3): exchange a username/password pair for a short-lived access token.",
      "properties": {
        "cache": {
          "$ref": "#/definitions/Cache",
          "description": "Cache, when set, writes the captured access token into a cache.\u003cname\u003e slot."
        },
        "client_id": {
          "$ref": "#/definitions/Value",
          "description": "ClientID is optional because some servers authenticate the client via Basic auth on the token endpoint."
        },
        "password": {
          "$ref": "#/definitions/Value",
          "description": "Password is the resource-owner password."
        },
        "scopes": {
          "description": "Scopes is the optional space-separated OAuth2 scope list.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "token_url": {
          "$ref": "#/definitions/Value",
          "description": "TokenURL is the OAuth2 token endpoint."
        },
        "username": {
          "$ref": "#/definitions/Value",
          "description": "Username is the resource-owner user name."
        }
      },
      "required": [
        "password",
        "token_url",
        "username"
      ],
      "type": "object"
    },
    "Path": {
      "description": "Namespace-rooted reference (schema/path.go). Dotted string (state.url, events.last.timestamp), {parts: [...]} escape form for segments containing dots, or null for the empty path. Roots: state, cache, events, extract, steps, response, or a fan_out.as alias.",
      "oneOf": [
        {
          "type": "string"
        },
        {
          "additionalProperties": false,
          "properties": {
            "parts": {
              "items": {
                "type": "string"
              },
              "minItems": 1,
              "type": "array"
            }
          },
          "required": [
            "parts"
          ],
          "type": "object"
        },
        {
          "type": "null"
        }
      ]
    },
    "Predicate": {
      "description": "Boolean condition (schema/predicate.go). Exactly one form: eq, gt, lt, gte, lte, present, and, or, not, literal_bool.",
      "oneOf": [
        {
          "additionalProperties": false,
          "properties": {
            "eq": {
              "$ref": "#/definitions/PredicateEq"
            }
          },
          "required": [
            "eq"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "gt": {
              "$ref": "#/definitions/PredicateEq"
            }
          },
          "required": [
            "gt"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "lt": {
              "$ref": "#/definitions/PredicateEq"
            }
          },
          "required": [
            "lt"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "gte": {
              "$ref": "#/definitions/PredicateEq"
            }
          },
          "required": [
            "gte"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "lte": {
              "$ref": "#/definitions/PredicateEq"
            }
          },
          "required": [
            "lte"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "present": {
              "$ref": "#/definitions/Path"
            }
          },
          "required": [
            "present"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "and": {
              "items": {
                "$ref": "#/definitions/Predicate"
              },
              "minItems": 1,
              "type": "array"
            }
          },
          "required": [
            "and"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "or": {
              "items": {
                "$ref": "#/definitions/Predicate"
              },
              "minItems": 1,
              "type": "array"
            }
          },
          "required": [
            "or"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "not": {
              "$ref": "#/definitions/Predicate"
            }
          },
          "required": [
            "not"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "literal_bool": {
              "type": "boolean"
            }
          },
          "required": [
            "literal_bool"
          ],
          "type": "object"
        }
      ]
    },
    "PredicateEq": {
      "additionalProperties": false,
      "properties": {
        "path": {
          "$ref": "#/definitions/Path"
        },
        "value": {
          "$ref": "#/definitions/Value"
        }
      },
      "required": [
        "path",
        "value"
      ],
      "type": "object"
    },
    "Progress": {
      "description": "Flat list of state writes evaluated after each accepted page-response.",
      "items": {
        "$ref": "#/definitions/ProgressWrite"
      },
      "type": "array"
    },
    "ProgressWrite": {
      "additionalProperties": false,
      "description": "ProgressWrite is one entry in Progress — a {to, from, coerce?, regex?} write evaluated per accepted page-response.",
      "properties": {
        "coerce": {
          "description": "Coerce, when set, applies a format verb to the resolved value before writing.",
          "type": "string"
        },
        "from": {
          "$ref": "#/definitions/Value",
          "description": "From is the Value to resolve and write. Has access to every namespace the request scope has (state, cache, events, response.body/header, steps.\u003cid\u003e.body/header)."
        },
        "regex": {
          "description": "Regex is the optional regular expression applied to the resolved string before writing.",
          "type": "string"
        },
        "to": {
          "$ref": "#/definitions/Path",
          "description": "To is the state.\u003cname\u003e destination (persistent — must be declared under state:)."
        }
      },
      "required": [
        "from",
        "to"
      ],
      "type": "object"
    },
    "Reducer": {
      "description": "Reducer operand: a bare array of Values (sugar for {list: [...]}) or a single list-shaped Value projection.",
      "oneOf": [
        {
          "items": {
            "$ref": "#/definitions/Value"
          },
          "type": "array"
        },
        {
          "$ref": "#/definitions/Value"
        }
      ]
    },
    "RegexExpr": {
      "additionalProperties": false,
      "properties": {
        "capture": {
          "description": "1-based capture-group index; 0/omitted returns the full match.",
          "type": "integer"
        },
        "default": {
          "$ref": "#/definitions/Value"
        },
        "from": {
          "$ref": "#/definitions/Value"
        },
        "pattern": {
          "description": "Go regular expression source.",
          "type": "string"
        }
      },
      "required": [
        "pattern",
        "from"
      ],
      "type": "object"
    },
    "Request": {
      "additionalProperties": false,
      "description": "Request describes a single HTTP request in the chain.",
      "properties": {
        "body": {
          "$ref": "#/definitions/Body",
          "description": "Body is the request body. Discriminated-union (json/form/raw)."
        },
        "cache": {
          "$ref": "#/definitions/Cache",
          "description": "Cache wraps the step in a generic step-level expiry cache. Mutually exclusive with FanOut."
        },
        "expect_status": {
          "description": "ExpectStatus is the set of status codes the runner treats as successful. Defaults to {200} when empty.",
          "items": {
            "type": "integer"
          },
          "type": "array"
        },
        "extract": {
          "description": "Extract pulls named values out of the response body or headers.",
          "items": {
            "$ref": "#/definitions/ExtractVar"
          },
          "type": "array"
        },
        "fan_out": {
          "$ref": "#/definitions/FanOut",
          "description": "FanOut lifts the step into a per-item iteration over a list Value. Mutually exclusive with Cache."
        },
        "headers": {
          "additionalProperties": {
            "$ref": "#/definitions/Value"
          },
          "description": "Headers is the wire header map; each value resolves to a string.",
          "type": "object"
        },
        "id": {
          "description": "ID is the optional step label. Required for steps.\u003cid\u003e.body.\u003cpath\u003e references and for fan_out.",
          "type": "string"
        },
        "if": {
          "$ref": "#/definitions/Predicate",
          "description": "If is the predicate that gates execution of the step."
        },
        "method": {
          "description": "Method is the HTTP verb (GET, POST, ...).",
          "enum": [
            "GET",
            "POST",
            "PUT",
            "PATCH",
            "DELETE",
            "HEAD"
          ],
          "type": "string"
        },
        "on_status": {
          "additionalProperties": {
            "enum": [
              "skip",
              "fail",
              "empty_events",
              "invalidate_cache"
            ],
            "type": "string"
          },
          "description": "OnStatus maps a specific HTTP status code to a per-step dispatcher verb (skip / fail / empty_events / invalidate_cache).",
          "propertyNames": {
            "pattern": "^[1-5][0-9][0-9]$"
          },
          "type": "object"
        },
        "produces_events": {
          "description": "ProducesEvents marks this step as the events producer. At most one in the chain; defaults to the last request.",
          "type": "boolean"
        },
        "query": {
          "additionalProperties": {
            "$ref": "#/definitions/Value"
          },
          "description": "Query is the URL query map; each value resolves to a string.",
          "type": "object"
        },
        "terminate_when": {
          "$ref": "#/definitions/Predicate",
          "description": "TerminateWhen is the request-level loop primitive: while the predicate is false, the same request is re-fired; when true the runner advances to the next request."
        },
        "url": {
          "$ref": "#/definitions/Value",
          "description": "URL is the absolute request URL Value."
        }
      },
      "required": [
        "method",
        "url"
      ],
      "type": "object"
    },
    "Response": {
      "additionalProperties": false,
      "description": "Response describes how to decode the producer step's body and locate the events list.",
      "properties": {
        "decode": {
          "description": "Decode is the body decoder verb: \"json\" or \"ndjson\".",
          "enum": [
            "json",
            "ndjson"
          ],
          "type": "string"
        },
        "events_at": {
          "$ref": "#/definitions/Path",
          "description": "EventsAt is the namespace-rooted Path locating the events list, rooted at response.body.\u003cpath\u003e or steps.\u003cid\u003e.body.\u003cpath\u003e. The zero (empty) Path means \"the body root IS the events list\"."
        }
      },
      "required": [
        "decode"
      ],
      "type": "object"
    },
    "SelectValue": {
      "additionalProperties": false,
      "properties": {
        "branches": {
          "items": {
            "additionalProperties": false,
            "properties": {
              "value": {
                "$ref": "#/definitions/Value"
              },
              "when": {
                "$ref": "#/definitions/Predicate"
              }
            },
            "required": [
              "when",
              "value"
            ],
            "type": "object"
          },
          "type": "array"
        },
        "default": {
          "$ref": "#/definitions/Value"
        }
      },
      "required": [
        "branches",
        "default"
      ],
      "type": "object"
    },
    "SigV4Auth": {
      "additionalProperties": false,
      "description": "SigV4Auth signs every request with AWS Signature Version 4. Region and Service are required. Credentials are taken from access_key_id + secret_access_key (with optional session_token); when all three are omitted, the AWS default credential chain is used (environment, shared config, IMDS / container / IAM role).",
      "properties": {
        "access_key_id": {
          "$ref": "#/definitions/Value",
          "description": "AccessKeyID is the AWS access key id. Optional: omit (with SecretAccessKey) to use the default credential chain."
        },
        "region": {
          "$ref": "#/definitions/Value",
          "description": "Region is the AWS region used in the credential scope (e.g. us-east-1)."
        },
        "secret_access_key": {
          "$ref": "#/definitions/Value",
          "description": "SecretAccessKey is the AWS secret access key. Required when AccessKeyID is set."
        },
        "service": {
          "$ref": "#/definitions/Value",
          "description": "Service is the AWS service name used in the credential scope (e.g. execute-api, es, s3)."
        },
        "session_token": {
          "$ref": "#/definitions/Value",
          "description": "SessionToken is the optional STS session token for temporary credentials."
        }
      },
      "required": [
        "region",
        "service"
      ],
      "type": "object"
    },
    "Value": {
      "description": "Universal dynamic-value type (schema/value.go). A scalar string, integer, boolean, or null; or a single-discriminator expression object (ref, now, concat, select, format, base64, list, object, add, subtract, max, min, first, last, count, regex). A string scalar is scanned for ${path} interpolation.",
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "integer"
        },
        {
          "type": "boolean"
        },
        {
          "type": "null"
        },
        {
          "additionalProperties": false,
          "properties": {
            "literal_string": {
              "type": "string"
            }
          },
          "required": [
            "literal_string"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "default": {
              "$ref": "#/definitions/Value"
            },
            "ref": {
              "$ref": "#/definitions/Path"
            }
          },
          "required": [
            "ref"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "now": {
              "const": true
            }
          },
          "required": [
            "now"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "concat": {
              "items": {
                "$ref": "#/definitions/Value"
              },
              "minItems": 2,
              "type": "array"
            }
          },
          "required": [
            "concat"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "select": {
              "$ref": "#/definitions/SelectValue"
            }
          },
          "required": [
            "select"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "format": {
              "description": "Format verb (string, int, bool, rfc3339, rfc3339nano, unix_seconds, unix_millis, duration, url_encode, parse_duration) or a Go date layout.",
              "type": "string"
            },
            "value": {
              "$ref": "#/definitions/Value"
            }
          },
          "required": [
            "format"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "base64": {
              "$ref": "#/definitions/Value"
            }
          },
          "required": [
            "base64"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "list": {
              "items": {
                "$ref": "#/definitions/Value"
              },
              "type": "array"
            }
          },
          "required": [
            "list"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "object": {
              "additionalProperties": {
                "$ref": "#/definitions/Value"
              },
              "type": "object"
            }
          },
          "required": [
            "object"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "add": {
              "items": {
                "$ref": "#/definitions/Value"
              },
              "maxItems": 2,
              "minItems": 2,
              "type": "array"
            }
          },
          "required": [
            "add"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "subtract": {
              "items": {
                "$ref": "#/definitions/Value"
              },
              "maxItems": 2,
              "minItems": 2,
              "type": "array"
            }
          },
          "required": [
            "subtract"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "max": {
              "$ref": "#/definitions/Reducer"
            }
          },
          "required": [
            "max"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "min": {
              "$ref": "#/definitions/Reducer"
            }
          },
          "required": [
            "min"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "first": {
              "$ref": "#/definitions/Reducer"
            }
          },
          "required": [
            "first"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "last": {
              "$ref": "#/definitions/Reducer"
            }
          },
          "required": [
            "last"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "count": {
              "$ref": "#/definitions/Reducer"
            }
          },
          "required": [
            "count"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "regex": {
              "$ref": "#/definitions/RegexExpr"
            }
          },
          "required": [
            "regex"
          ],
          "type": "object"
        }
      ]
    }
  },
  "description": "Doc is the top-level IR document.",
  "properties": {
    "auth": {
      "$ref": "#/definitions/Auth",
      "description": "Auth is the discriminated-union auth block. Exactly one variant."
    },
    "error": {
      "$ref": "#/definitions/ErrorBlock",
      "description": "Error configures how non-success HTTP responses are surfaced. Optional."
    },
    "ir_version": {
      "const": "1",
      "description": "IRVersion is the spec wire-format version. Must equal IRVersion.",
      "type": "string"
    },
    "pagination": {
      "$ref": "#/definitions/Pagination",
      "description": "Pagination is the discriminated-union pagination block. Exactly one variant."
    },
    "progress": {
      "$ref": "#/definitions/Progress",
      "description": "Progress is the flat list of state writes evaluated after each accepted page-response. An empty list (or omitted block) means no progress tracking."
    },
    "requests": {
      "description": "Requests is the ordered list of HTTP requests run on every iteration.",
      "items": {
        "$ref": "#/definitions/Request"
      },
      "type": "array"
    },
    "response": {
      "$ref": "#/definitions/Response",
      "description": "Response describes how to decode the producer step's body and where the events list lives."
    },
    "state": {
      "additionalProperties": {
        "$ref": "#/definitions/FieldDecl"
      },
      "description": "State is the flat map of typed field declarations. Optional — authors with no state at all may omit the block.",
      "type": "object"
    }
  },
  "required": [
    "auth",
    "ir_version",
    "pagination",
    "requests",
    "response"
  ],
  "title": "Skopos spec (ir_version 1)",
  "type": "object"
}
